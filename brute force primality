#include<stdio.h>

int main()
{
    int n;
    scanf("%d",&n);
    int count=0;
    for(int i=2;i<=n/2;i++)
    {
        if(n%i==0)
        count=1;
    }
    if(count==1)
    printf("composite");
    else
    printf("prime");

}
